# Python dependencies, provided by the environment (rez package or
# site-packages). Listed here as a reference, not for pip installation.
fastapi>=0.110
uvicorn>=0.29
faster-whisper>=1.0
imageio-ffmpeg>=0.4
argostranslate>=1.9
